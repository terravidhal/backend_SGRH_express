const routes = [
    { path: '/', route: 'index' },
    { path: '/auth', route: 'auth/auth' },
    { path: '/logs', route: 'logs' },
    { path: '/tasks', route: 'tasks' },
    { path: '/notifs', route: 'notifs' },
];

module.exports = routes;